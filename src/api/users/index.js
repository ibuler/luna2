import {
  getProfile,
  getUnreadCount,
  getMessage,
  getContentByMsgId,
  hasRead,
  removeReaded,
  restoreTrash
} from './users'

const users = {
  getProfile,
  getUnreadCount,
  getMessage,
  getContentByMsgId,
  hasRead,
  removeReaded,
  restoreTrash
}

export default users
