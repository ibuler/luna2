import {
  getProfile,
  getUserList,
  getUnreadCount,
  getMessage,
  getContentByMsgId,
  hasRead,
  removeReaded,
  restoreTrash
} from './users'

const users = {
  getProfile,
  getUserList,
  getUnreadCount,
  getMessage,
  getContentByMsgId,
  hasRead,
  removeReaded,
  restoreTrash
}

export default users
